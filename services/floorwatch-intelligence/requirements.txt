fastapi>=0.110
uvicorn[standard]>=0.52.3
httpx>=0.27
numpy>=1.24
scikit-learn>=1.3
mcp>=1.0
psycopg[binary]>=3.1
pgvector>=0.2
pyjwt>=2.8
python-dotenv>=1.0
anthropic>=0.30
voyageai>=0.2

# Only needed for FLOORWATCH_LLM_PROVIDER=openai or =gemini (see
# app/llm.py's module docstring) — both are imported lazily, so leave
# these uninstalled if you're using the default "anthropic" provider.
# openai>=1.0          # FLOORWATCH_LLM_PROVIDER=openai — ChatGPT, or any
                        #   OpenAI-compatible host via FLOORWATCH_LLM_BASE_URL
                        #   (Kimi/Moonshot, DeepSeek, Groq/Together/Ollama-
                        #   hosted Llama, etc.)
# google-genai>=0.3     # FLOORWATCH_LLM_PROVIDER=gemini
