# Test-only dependencies — not needed at runtime (see ../requirements.txt).
pytest>=8.0
pytest-asyncio>=0.23
pytest-timeout>=2.3
httpx>=0.27

# google-genai and openai are otherwise optional, lazily-imported runtime
# dependencies (see ../requirements.txt's FLOORWATCH_LLM_PROVIDER comment
# block) — but test_llm.py's provider-dispatch tests exercise both import
# paths directly with no importorskip guard, so both need to actually be
# installed for those tests to run (rather than hard-fail with
# ModuleNotFoundError) in a clean environment like CI. Verified by running
# the full suite in an isolated venv seeded from only this file plus
# ../requirements.txt — both gaps were found that way, not guessed.
google-genai>=0.3
openai>=3.0.0
