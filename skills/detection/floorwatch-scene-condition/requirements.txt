# Floorwatch Scene-Condition — provider-agnostic vision judgment.
# openai covers OpenAI, Grok (xAI), and any OpenAI-compatible Llama host
# (Together/Fireworks/Groq/Deepinfra/...) — same client, different base_url.
# anthropic is only imported if --provider claude/anthropic is actually used.
redis>=5.0
pydantic>=2.0
openai>=1.0
anthropic>=0.40
